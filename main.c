#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "func.h"
//to compile gcc main.c func.c -o prg.exe don't need .exe if you are on mac
/*
# Construire un programme permettant la saisie d'une chaîne de 
# caractères et permutant ses deux moitiés. Afficher le résultat.
# "abcdef" donne par exemple "defabc", "abcdefg" donne par exemple "defgabc".
*/
int main(int argc, char ** argv) {
    menu();
    return 0;
}