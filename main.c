#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "func.h"
/*
# Une drôle d'imprimante circulaire a été inventée récemment. 
# Elle possède un pointeur sur la lettre A lorsqu’onl'allume, 
# et déplacer ce pointeur d'une lettre 
# (par exemple de A vers B ou de A vers Z) 
# prend exactement uneseconde.
*/
//to compile gcc main.c func.c -o prg.exe don't need .exe if you are on mac
int main(int argc, char ** argv) {
    return 0;
}