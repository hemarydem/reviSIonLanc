#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "func.h"
//to compile gcc main.c func.c -o prg.exe don't need .exe if you are on mac
//["chemise fantaisie", "pantalon velours", "chemise fantaisie", "tongues", "bluejeans", "bluejeans"]
int main(int argc, char ** argv) {
    return 0;
}